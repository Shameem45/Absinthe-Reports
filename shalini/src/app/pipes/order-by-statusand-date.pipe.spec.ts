import { OrderByStatusandDatePipe } from './order-by-statusand-date.pipe';

describe('OrderByStatusandDatePipe', () => {
  it('create an instance', () => {
    const pipe = new OrderByStatusandDatePipe();
    expect(pipe).toBeTruthy();
  });
});
